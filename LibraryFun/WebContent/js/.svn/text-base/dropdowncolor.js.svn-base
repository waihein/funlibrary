function setColor()
{
/*
all the values from selector come in as array. need to use[myselect.selectedIndex]
*/
	var myselect = document.getElementById("colorselector").value;
	//colour = myselect.options[myselect.selectedIndex].className;
	//alert(colour);
	//myselect.className = colour;
	//myselect.blur(); //This just unselects the select list without 
	document.body.style.backgroundColor= myselect;

}

